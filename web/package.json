{
  "name": "transferia-control-plane-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:api": "node scripts/generate-api.mjs",
    "build": "npm run generate:api && tsc --noEmit && node scripts/build.mjs",
    "pretest": "npm run generate:api",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "10.29.8"
  },
  "devDependencies": {
    "@testing-library/preact": "3.2.4",
    "esbuild": "0.28.2",
    "jsdom": "30.0.1",
    "prettier": "3.8.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
